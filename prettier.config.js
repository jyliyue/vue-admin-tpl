module.exports = {
    // 使用单引号, 默认false(在jsx中配置无效, 默认都是双引号)
    singleQuote: true,
    // 句末加分号
    semi: false,
    // 最后一个对象元素加逗号
    trailingComma: 'none',
    // 4 缩进
    tabWidth: 4,
    // 禁止使用 tab
    useTabs: false,
    //对象大括号直接是否有空格，默认为true，效果：{ foo: bar }
    bracketSpacing: true
}
